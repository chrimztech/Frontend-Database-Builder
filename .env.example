# CeMIS Spring Boot backend — public, safe to commit (already in .env)
VITE_API_URL="http://localhost:8080/api"

# ── Secrets: copy these lines into .env.local (never commit .env.local) ──────

# ── Email (SMTP) — for sending certificates to students ──────────────────────
# Use your institution's outgoing mail server settings.
# Example for a typical campus SMTP relay:
SMTP_HOST="smtp.unza.ac.zm"
SMTP_PORT="587"
SMTP_SECURE="false"          # set to "true" only if port 465 (SSL)
SMTP_USER="train@unza.ac.zm"
SMTP_PASS="<your email password>"
SMTP_FROM="UNZA TeLS <train@unza.ac.zm>"
